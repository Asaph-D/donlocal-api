apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: donlocal-api-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: donlocal-api
            port:
              number: 80